//first of i insatalled git(a version control system), i got a new terminal gitbash
//checked its proper installtion by command git --version
//configured git by following commands
//git config --global user.name "Jagdish Patidar"
//git config --global user.email "patidar.jg@gmail.com" 
// checked them by git config --list

//created directory FirstGitFromTerminal by mkdir
//went into that new directory by cd command
//to check in which directory u are use pwd command

//initialise this directory for git by using command git init
//to check this action used cmd git status

//created this text file and pressed the save button.
//now added this file to git for tracking its changes from now by command git add GitProgram.txt

//checked this action by git status ... terminal displayd "changes to be committed in new file GitProgram.txt (means now it is in staging area)

//committed the changes by git commit -m "first commit"
//terminal showed 1 file changed, 15 inserstions+
//checked this action by git status,,found nothing to commit.. means ok
//also checked by git log

//now i am again saving the changes in this text file.
//checked the git status.. terminal showed Changes not staged for commit:modified: GitProgram.txt
//so i have to add again the file for staging and the committing
//used git add GitProgram.txt and then git commit -m "second committ"
//checked by git log.. terminal showing list of all commits with hashcode

//checkout command to go to previous version, using git checkout <hashcode>
//firstly i am saving this file here... i also added and committed this changes as third commit
//i checked out to first commit, it was awesome..
//to get back to the last saved changes, use git checkout main.
//and here i am saving the changes, adding to git and committing as fourthnfinal commit.



//now understanding GitHub
//git is in local machine, GitHub is the concept of git on remote server
//so created a account on GitHub, and created a new repository named FirstGitOnGithub
//after creating, in Quick Setup following the'''' push an existing repository from the command line''' instructions

//now in gitbash i will check is there any remote location already?
//by using command git remote -v, it showing nothing means no already
//i disconnected internet
//so following first instruction. type command git remote add origin https://github.com/jagdishworld/FirstGitOnGithub.git
//now checking by git remote -v, terminal showing push and fetch, means ok

//following second instruction git branch -M main
//following third instruction git push -u origin main
// now here internet is must required, i connected to the internt
//but now error comes with popup window, this module is blocked from loading into the local security authority, and also GitHub login window open, i authorize the access by login
//then wow.. success.. i refreshed GitHub. the all commits history showed in website.

//now i m chckin git status and log. terminal showing nothing to commit. git log showing all 4 commits
//now i am saving, adding and commiting the changes as "firstremotecommit"
//now i checked on the GitHub, there is no "firstremotecommit"
//i have to use cmd git push, and i refreshed GitHub, i found the firstremotecommit. success.
//so finally i am saving this changes in text editor, and adding to git and committing as "finalremotecommit"

//now i am curious that can edit is possible directly from github. so i found editor, and i m adding this line from github. and lets commit the changes
//its asking commit name, i am giving "FirstGithubCommit"

now i was thinking how this change made in github will reflect in local machine,
so used cmd in gitbash git pull origin main, but after navigating in that repository folder and verifying the connection by cmd git remote -v(it showed push,fetch means ok)
IMP. your local Git repository is linked to a remote repository configuration using the shortname "origin". Git dont allow multiple remote links to share the exact same shortname within a single local repository

thus checkd the changes to local machine, now committing this by naming "SyncToLocalMachine"
